{% extends '@EasyAdmin/page/content.html.twig' %}

{% block content %}
    <div class="content">
        <div class="container-fluid">
            <div class="row">
                <div class="col-md-12">
                    <div class="card">
                        <div class="card-body">
                            <h1 class="mb-4">Bienvenue dans votre espace administrateur!</h1>

                            <h2 class="mb-3">Utilisateurs:</h2>
                            <div class="table-responsive">
                                {{ easy_admin_render_list(User, {
                                    'search': true,  
                                    'paginator': true  
                                }) }}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
