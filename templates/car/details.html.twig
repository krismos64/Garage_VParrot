{# templates/car/cardetails.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}Détails de la Voiture{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Détails de la Voiture</h1>

        <div class="card">
            <div class="card-body">
                <h5 class="card-title">{{ car.brand }} {{ car.model }}</h5>
                <p class="card-text"><strong>Année:</strong> {{ car.year }}</p>
                <p class="card-text"><strong>Kilométrage:</strong> {{ car.km }} km</p>
                <p class="card-text"><strong>Prix:</strong> {{ car.price }}€</p>
                <p class="card-text"><strong>Description:</strong> {{ car.description }}</p>

                <!-- Display car images as thumbnails -->
                {% if car.images|length > 0 %}
                    <h5 class="mt-3">Images de la Voiture</h5>
                    <div class="row">
                        {% for image in car.images %}
                            <div class="col-md-4 mb-3">
                                <div class="card">
                                    <img src="{{ asset('/images/Cars/' ~ image.title) }}" class="card-img-top" alt="{{ image.title }}">
                                    <div class="card-body">
                                        <p class="card-text">{{ image.description }}</p>
                                    </div>
                                </div>
                            </div>
                        {% endfor %}
                    </div>
                {% endif %}
            </div>
        </div>
    </div>
{% endblock %}

